[package]
name = "testserver"
version = "0.0.0"
description = "HTTP server for testing."
edition = "2021"
publish = false

[dependencies]
humantime = "2"
regex = "1.12"
threadfin = "0.1.1"
tiny_http = "0.12"
