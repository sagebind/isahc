[package]
name = "testserver"
version = "0.0.0"
description = "HTTP server for testing."
edition = "2018"
publish = false

[dependencies]
humantime = "2"
once_cell = "1"
regex = "1.3"
threadfin = "0.1.1"
tiny_http = "0.12"
